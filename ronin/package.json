{
  "name": "ronin-ctf-theme",
  "version": "1.0.0",
  "description": "RONIN CTF — Way of the Cyber Ronin. A CTFd theme.",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node -e \"require('fs').copyFileSync('static/.vite/manifest.json', 'static/manifest.json')\""
  },
  "devDependencies": {
    "sass": "^1.70.0",
    "vite": "^5.0.0"
  }
}
